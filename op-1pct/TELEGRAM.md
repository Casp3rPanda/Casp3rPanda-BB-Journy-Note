# Telegram — last good state (Day 2)

The bot is not dead. `/status` answered 2026-09-17. Worker ON. Overnight ON. Hunt arm false.

## What died
The hunter loop died on ClickHouse, not on BotFather. Region-scope broke the desk: listed Langfuse EU vs opened US. Live `/scope` never finished. 0 hops scored. Catalog `/programs` kept looking like work. A locked empty desk is not a hunt.

## Last good state
Go PBB worker still boots. Commands that work: `/status` `/programs` `/hold` `/pick` `/scope` `/log` `/check`. Last pick was `bc-clickhouse`. Last scope: none. Reps 0/6. Git ledger live (`Casp3rPanda/Casp3rPanda-BB-Journy-Note`). Do not paste chat ids or tokens here.

## Next fix
Type `/hold bc-clickhouse region-scope break` if Telegram does not already show it held. Then `/pick h1-faraday_inc`, paste live policy, `/scope done`, `/check app.faraday.ai`, `/check api.faraday.ai`. Two jars you own. One dummy. Cookie-swap B once. `/log`. That is the rebuild: use the radio, do not redesign it.
