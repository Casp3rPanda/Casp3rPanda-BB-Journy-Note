# Diary 2026-09-18

Day 2. Goals only: IDOR / BAC / XSS payouts → OSCP at month 18.
BAC = Broken Access Control.

## Git
- [x] Ledger on Casp3rPanda/Casp3rPanda-BB-Journy-Note
- [x] Day 1 diary: region/host law
- [x] ClickHouse HOLD written
- [x] Files cleaned (ledger + nobleNote index / hunt-bar / `_not-hunted` README / botlog)

## ClickHouse — abandoned
- Desk: `bc-clickhouse` — **HOLD / done**
- Why: region-scope break. EU `cloud.langfuse.com` vs US `us.cloud.langfuse.com`. Unlisted region is not payable.
- Live `/scope`: never finished. Hops: **0**. Fileable: **none**.
- Lesson: exact host + region or stop. See Diary 2026-09-17.

## Telegram (3 lines)
What died: the ClickHouse desk, not the bot.
Last good: `/status` 2026-09-17, worker on, 0 hops, last scope none.
Next: `/hold` if needed → `/pick h1-faraday_inc` → live `/scope` → `/check` both Faraday hosts → one hop `/log`.

## Next programs (live policy read 2026-09-18 — still paste in Telegram)
Not locked. You `/pick`. Overnight skeletons in `_not-hunted/` are not desks.

| Pick | Why | Exact hosts (policy) | Do not |
|------|-----|----------------------|--------|
| **`h1-faraday_inc` (recommended)** | Public signup. Two jars. Cross-account IDOR/BAC/XSS is the stated impact. Unlisted subdomain = OOS — same law as ClickHouse. | `app.faraday.ai` `api.faraday.ai` · signup `app.faraday.ai/signup` | Unlisted Faraday subdomain. Parent/sub account pair as “cross-user.” Own JWT 200. `faraday.atlassian.net`. Demo on marketing site. |
| `h1-pingidentity` | Named SaaS only. | `console.ort-one-pingone.com` `openam-bug-bounty-stag.forgeblocks.com` | `*.pingidentity.*` `*.symphonicsoft.*` and any other Ping-registered domain not in the asset list. Creds from H1 Credential Management / policy — not from this repo. |
| `h1-insightly` | Two free trials = two tenants. Classic IDOR/BAC. | Confirm live Scope tab before login. `api.insightly.com/v3.0` and older are OOS. | Same-tenant “attack yourself.” |

Braze (`h1-braze_inc`): **not tonight.** Three hosts only, all `k8s.tools-001.d-use-1.braze-dev.com` (US-East). Access-control/permissions OOS as of 2026-02-06. Cross-org required. Region lesson is already written; Faraday is the hunt.

Held — do not pick: Box, Freshworks, TikTok, Enjin, ClickHouse, HackenProof.

## Hop
Not run. Cleanup + next-desk list is the session.

## After `/pick`
Telegram: paste live policy → `/scope done` → `/check` each host.
Then dummy as A → History GET → cookie-swap B once → `/log`.
Reply statuses A / B / unauth (no cookies). Private bytes: yes/no.
